# Copy this file to `app.env` in the same directory and fill in your own key.
# `app.env` is git-ignored; this template is not.
# Get a TMDB v3 API key at https://www.themoviedb.org/settings/api
TMDB_API_KEY=
